The `skeleton` directory contains files which should be aligned across all
packages in the repo.

There is a separation between `shared`, `servers` and `modules` allowing files
to be scoped to the different cases.
